version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused

issues:
  exclude-use-default: false
  max-issues-per-linter: 50
  max-same-issues: 3
